# 仿真运维经理 - 业务流程文档

| 版本 | 日期 | 作者 | 变更说明 |
|-----|------|-----|---------|
| v0.1 | 2024-03-20 | CRT | 初稿，核心业务流程 |

## 1. 核心业务流程

### 1.1 准入检查全流程

```mermaid
flowchart TD
    A[创建准入检查任务] --> B[初始化检查清单]
    B --> C[台账收集阶段]
    C --> D{台账是否完整?}
    D -->|否| C
    D -->|是| E[基础资源验证阶段]
    E --> F[执行基线验证脚本]
    F --> G{基线是否合规?}
    G -->|否| H[问题整改]
    H --> F
    G -->|是| I[软件部署验证阶段]
    I --> J{部署是否合规?}
    J -->|否| H
    J -->|是| K[系统安全验证阶段]
    K --> L{安全是否合规?}
    L -->|否| H
    L -->|是| M[监控告警配置阶段]
    M --> N{监控是否配置?}
    N -->|否| H
    N -->|是| O[生成准入报告]
    O --> P[准入审批]
    P --> Q{审批通过?}
    Q -->|否| H
    Q -->|是| R[准入完成，允许上线]
```

**流程说明**:
1. **创建准入检查任务**: 运维经理创建任务，选择系统、版本、上线时间，系统自动加载预设检查模板
2. **初始化检查清单**: 根据模板生成结构化的检查项，分配给相关责任人
3. **台账收集阶段**: 收集应用系统、云服务、账户三类台账信息，上传交付物
4. **基础资源验证阶段**: 通过脚本验证系统基线配置（账户、磁盘、网络、环境）
5. **软件部署验证阶段**: 验证软件安装路径、权限、安装脚本、日志记录
6. **系统安全验证阶段**: 安全检查（待细化）
7. **监控告警配置阶段**: 监控配置检查（待细化）
8. **生成准入报告**: 汇总所有检查结果，生成准入报告
9. **准入审批**: 多级审批流程
10. **准入完成**: 所有检查通过，系统允许上线

### 1.2 台账提交流程

```mermaid
flowchart TD
    A[开始台账收集] --> B[填写应用系统台账]
    B --> C[填写云服务开通台账]
    C --> D[填写系统账户台账]
    D --> E[上传交付物]
    E --> F{数据完整性校验}
    F -->|不完整| G[提示补充]
    G --> B
    F -->|完整| H[提交审核]
    H --> I[运维经理审核]
    I --> J{审核结果}
    J -->|驳回| K[查看驳回原因]
    K --> B
    J -->|通过| L[台账确认完成]
```

**台账数据流**:
- 系统管理员/开发团队填写台账数据
- 系统执行字段格式校验（IP格式、日期格式等）
- 必填字段完整性检查
- 运维经理审核台账准确性
- 审核通过后标记为"已确认"

### 1.3 基线验证脚本执行流程

```mermaid
sequenceDiagram
    actor U as 运维经理
    participant F as 前端
    participant A as API服务
    participant S as 脚本仓库
    participant T as 目标服务器
    participant D as 数据库
    
    U->>F: 选择验证目标服务器
    F->>A: 请求执行基线验证
    A->>S: 获取验证脚本
    S-->>A: 返回脚本内容
    A->>T: SSH/Agent下发并执行脚本
    T->>T: 本地执行检查
    T-->>A: 返回JSON格式结果
    A->>A: 解析验证结果
    A->>D: 存储验证记录
    A-->>F: 返回结构化结果
    F-->>U: 展示验证报告
```

**脚本验证数据流**:
1. 运维经理在界面上选择要验证的服务器IP
2. 系统通过SSH或Agent在目标服务器执行脚本
3. 脚本收集系统配置信息，与基线要求对比
4. 返回结构化JSON结果（通过/失败/警告）
5. 前端可视化展示验证结果
6. 验证记录存储到数据库，可追溯

### 1.4 交付物提交流程

```mermaid
flowchart TD
    A[选择检查项] --> B[选择交付物类型]
    B --> C[上传文件]
    C --> D[病毒扫描]
    D --> E{是否安全?}
    E -->|否| F[拒绝上传，提示风险]
    E -->|是| G[存储文件]
    G --> H[生成文件索引]
    H --> I[关联检查项]
    I --> J[标记检查项为<br/>"已提交交付物"]
    J --> K[运维经理审核]
    K --> L{审核结果}
    L -->|驳回| M[查看原因，重新上传]
    M --> B
    L -->|通过| N[标记检查项完成]
```

**交付物类型支持**:
| 检查项 | 交付物类型 | 格式要求 |
|-------|-----------|---------|
| 系统基线 | 操作手册/镜像说明 | Word/PDF |
| 软件部署 | 操作手册/脚本/安装日志 | Word/PDF/SH/LOG |
| 台账收集 | 原始表格 | Excel |

## 2. 状态机

### 2.1 准入检查任务状态流转

```mermaid
stateDiagram-v2
    [*] --> 草稿: 创建任务
    草稿 --> 进行中: 启动检查
    进行中 --> 待审核: 所有检查项完成
    待审核 --> 审批中: 提交审批
    审批中 --> 已通过: 审批通过
    审批中 --> 已驳回: 审批驳回
    已驳回 --> 进行中: 整改后重新提交
    已通过 --> [*]: 允许上线
    进行中 --> 已取消: 取消任务
    待审核 --> 已取消: 取消任务
    已取消 --> [*]
```

**状态说明**:
- **草稿**: 任务创建但未正式启动，可修改配置
- **进行中**: 检查正在进行，各责任人执行检查项
- **待审核**: 所有检查项已完成，等待运维经理审核
- **审批中**: 已提交正式审批流程
- **已通过**: 准入检查完成，系统可上线
- **已驳回**: 存在问题需要整改
- **已取消**: 任务被取消（系统不上线或延期）

### 2.2 检查项状态流转

```mermaid
stateDiagram-v2
    [*] --> 未开始: 任务初始化
    未开始 --> 进行中: 开始执行
    进行中 --> 待审核: 提交交付物
    待审核 --> 已通过: 审核通过
    待审核 --> 已驳回: 审核驳回
    已驳回 --> 进行中: 重新执行
    已通过 --> [*]
    进行中 --> 不适用: 标记NA
    不适用 --> [*]
```

### 2.3 台账记录状态

```mermaid
stateDiagram-v2
    [*] --> 待填写: 创建台账条目
    待填写 --> 填写中: 开始编辑
    填写中 --> 已提交: 提交审核
    已提交 --> 已确认: 审核通过
    已提交 --> 需修改: 审核驳回
    需修改 --> 填写中: 修改
    已确认 --> 已过期: 信息过期（如账户过期）
    已过期 --> 填写中: 更新信息
```

## 3. 时序图

### 3.1 台账录入与验证

```mermaid
sequenceDiagram
    actor Dev as 开发人员
    actor OM as 运维经理
    participant FE as 前端
    participant API as API服务
    participant DB as 数据库
    participant File as 文件存储
    
    Dev->>FE: 打开台账录入页面
    FE->>API: 获取台账模板
    API-->>FE: 返回台账结构
    FE-->>Dev: 显示录入表单
    
    Dev->>FE: 填写台账数据
    Dev->>FE: 上传交付物文件
    FE->>API: 上传文件请求
    API->>File: 存储文件
    File-->>API: 返回文件URL
    API-->>FE: 上传成功
    
    Dev->>FE: 提交台账
    FE->>API: 提交台账数据
    API->>API: 数据完整性校验
    API->>DB: 存储台账数据
    DB-->>API: 存储成功
    API-->>FE: 提交成功
    FE-->>Dev: 提示提交成功
    
    OM->>FE: 查看待审核台账
    FE->>API: 查询待审核列表
    API->>DB: 查询数据
    DB-->>API: 返回数据
    API-->>FE: 返回台账列表
    FE-->>OM: 展示审核界面
    
    OM->>FE: 审核通过
    FE->>API: 提交审核结果
    API->>DB: 更新台账状态
    API-->>FE: 审核完成
    FE-->>Dev: 通知审核通过
```

### 3.2 基线验证执行

```mermaid
sequenceDiagram
    actor OM as 运维经理
    participant FE as 前端
    participant API as API服务
    participant Script as 脚本引擎
    participant Target as 目标服务器
    participant DB as 数据库
    
    OM->>FE: 选择服务器执行基线验证
    FE->>API: 发送验证请求<br/>{server_ip, script_id}
    API->>DB: 查询脚本内容
    DB-->>API: 返回脚本
    API->>Script: 下发执行请求
    
    Script->>Target: SSH连接
    Script->>Target: 上传并执行脚本
    Target->>Target: 收集系统信息
    Target->>Target: 对比基线要求
    Target-->>Script: 返回验证结果JSON
    
    Script->>API: 返回结构化结果
    API->>DB: 存储验证记录
    API-->>FE: 返回验证报告
    FE-->>OM: 展示验证结果
    
    alt 验证通过
        OM->>FE: 确认检查项完成
        FE->>API: 更新检查项状态
        API->>DB: 更新状态为"已通过"
    else 验证失败
        OM->>FE: 查看失败详情
        FE->>OM: 显示修复建议
        OM->>Target: 人工修复问题
        OM->>FE: 重新执行验证
    end
```

## 4. 泳道图

### 4.1 准入检查多角色协作

```mermaid
flowchart LR
    subgraph 运维经理
        A1[创建准入任务]
        A2[分配检查项]
        A3[审核台账]
        A4[审核交付物]
        A5[审批准入报告]
    end
    
    subgraph 系统管理员
        B1[填写台账信息]
        B2[执行验证脚本]
        B3[上传操作手册]
    end
    
    subgraph 开发团队
        C1[提供系统信息]
        C2[上传安装脚本]
        C3[提交安装日志]
    end
    
    subgraph 安全团队
        D1[审核安全项]
    end
    
    subgraph 自动化系统
        E1[自动执行脚本]
        E2[生成检查报告]
    end
    
    A1 --> A2
    A2 --> B1
    A2 --> C1
    B1 --> A3
    C1 --> A3
    A3 --> B2
    B2 --> E1
    E1 --> A4
    B2 --> A4
    C2 --> A4
    C3 --> A4
    A4 --> D1
    D1 --> E2
    E2 --> A5
```

## 5. 异常处理

| 异常场景 | 触发条件 | 处理方式 | 用户提示 |
|---------|---------|---------|---------|
| 台账数据不完整 | 提交时必填字段缺失 | 阻断提交，高亮缺失字段 | "请完善以下必填项：IP地址、主机名" |
| 文件上传失败 | 网络中断/文件过大 | 支持断点续传/分片上传 | "上传中断，点击继续上传" |
| 脚本执行超时 | 服务器无响应超过5分钟 | 自动终止，记录超时日志 | "脚本执行超时，请检查服务器连接" |
| 脚本执行失败 | 服务器权限不足 | 记录错误输出，提示常见原因 | "执行失败：权限不足，请确认执行账户有sudo权限" |
| 台账信息过期 | 账户有效期到期前7天 | 标记台账状态为"即将过期" | "以下账户将在7天内过期，请更新" |
| 审批驳回 | 交付物不符合要求 | 打回检查项，附带驳回原因 | "操作手册缺少回滚步骤，请补充后重新上传" |
