# ============================================
# 生产环境依赖 - Serv00 (FreeBSD)
# 版本已锁定，确保部署一致性
# 注意：Serv00 使用 Python 3.9，部分依赖版本需兼容
# ============================================

# FastAPI框架
fastapi==0.110.3
uvicorn[standard]==0.29.0

# 数据库
sqlalchemy==2.0.30
alembic==1.13.1

# 安全
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# 数据验证
pydantic[email]==2.7.1

# 异步文件操作
aiofiles==23.2.1

# 工具
python-dotenv==1.0.1

# 测试依赖（生产环境也需要，用于健康检查）
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
